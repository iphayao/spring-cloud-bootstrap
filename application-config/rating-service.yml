server:
  port: 8082

spring:
  application:
    name: rating-service
  redis:
    host: localhost
    port: 6379

eureka:
  client:
    region: default
    registra-fetch-interval-seconds: 5

management:
  security:
    session: never